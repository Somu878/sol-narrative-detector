# Solana Devnet Wallet Private Key (base58 encoded)
# Generate with: solana-keygen new -o wallet.json (devnet), then convert to base58
PRIVATE_KEY=

# Groq API Key (get one free at https://console.groq.com/keys)
GROQ_API_KEY=

# Polling interval in minutes (default: 30)
POLLING_INTERVAL=30

# Devnet RPC URL (uncomment one)
# Option 1: Standard (may have rate limits)
# RPC_URL=https://api.devnet.solana.com
# Option 2: Alchemy (free tier)
RPC_URL=https://solana-devnet.g.alchemy.com/v2/demo

# Telegram Bot Notifications (optional)
# 1. Message @BotFather on Telegram, create a bot, and copy the token
# 2. Add the bot to your group/channel, or message it directly
# 3. Get your chat ID by messaging @userinfobot or from the Telegram API
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Upstash Redis (required for GitHub Actions / serverless — free at https://upstash.com)
# Creates persistent history across runs (local runs fallback to data/history.json)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
